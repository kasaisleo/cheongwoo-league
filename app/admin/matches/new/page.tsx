/**
 * /admin/matches/new — 관리자 경기 입력 페이지.
 *
 * 권한: 상위 app/admin/matches/layout.tsx의 requireAdminAccess()에서 처리.
 * UI: 기존 /matches/new 폼을 그대로 재사용.
 */
export { default } from "@/app/(public)/matches/new/page";
